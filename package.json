{
  "name": "download-cleaner-lite",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json *.ts lib/*.ts"
  },
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "^3.7.2",
    "web-ext-types": "^3.2.1"
  }
}
