{
  "name": "download-cleaner-lite",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json *.ts lib/*.ts"
  },
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.1.6",
    "web-ext-types": "^3.0.0"
  }
}
