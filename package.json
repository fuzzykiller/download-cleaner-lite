{
  "name": "download-cleaner-lite",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json *.ts lib/*.ts"
  },
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^2.9.2",
    "web-ext-types": "^2.1.0"
  }
}
